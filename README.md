Small autoclicker for windows.

## Compling
Prerequisites: 
 * [cmake](https://cmake.org)
 * MinGW


```
$ git clone https://github.com/Blaizick/Autoclicker
$ cd Autoclicker
$ mkdir build && cd build
$ cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release ..
$ cmake --build .  
```
